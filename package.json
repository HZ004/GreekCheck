{
  "name": "upstox-option-greeks-dashboard",
  "version": "1.0.0",
  "description": "React dashboard for visualizing Upstox Option Greeks data from S3 with interval granularity and date filtering",
  "scripts": {
    "start": "vite",
    "build": "vite build",
    "preview": "vite preview --host 0.0.0.0 --port $PORT"
  },
  "dependencies": {
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "recharts": "^2.6.2",
    "papaparse": "^5.3.1",
    "react-datepicker": "^4.15.0",
    "date-fns": "^2.30.0"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.0.0",
    "vite": "^4.0.0"
  },
  "engines": {
    "node": ">=18"
  }
}
